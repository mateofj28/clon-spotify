import 'package:flutter/material.dart';

class AppColors {
  // 🎯 Color principal de la marca (Spotify Green)
  static const Color primaryGreen = Color(0xFF1ED760);
  // 🎵 Fondo base tipo Spotify
  static const Color spotifyBackground = Color(
    0xFF191414,
  ); // Fondo oscuro predominante
  // Usado para botones principales, íconos activos, etc.

  // 🎨 Gris claro para bordes, texto secundario, separadores
  static const Color lightGray = Color(
    0xFFCECECE,
  ); // Usado para divisores, iconos desactivados, texto auxiliar

  // 🖤 Negro puro, fondo general de la app en modo oscuro
  static const Color black = Color(
    0xFF000000,
  ); // Usado como fondo principal en pantallas y cards

  // 🤍 Blanco puro, texto o íconos sobre fondo negro
  static const Color white = Color(
    0xFFFFFFFF,
  ); // Usado para texto principal, íconos, y fondos claros
}
